[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: every unknown path serves index.html so /account/*, /admin/*,
# /pay/:token etc. survive a direct load or page refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
